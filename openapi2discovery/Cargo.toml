[package]
name = "openapi2discovery"
version = "0.1.0"
edition = "2021"
description = "Convert OpenAPI 3.x specs to Discovery-style nested JSON schemas"

[dependencies]
openapiv3 = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
